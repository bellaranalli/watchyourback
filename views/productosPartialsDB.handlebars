<div>
  <h2 class="titulo1">PRODUCTOS</h2>
</div>

<table>
  <thead>
    <tr>
      <p class="perfil"><a href="http://localhost:8080/private.html" role="button" class="boton1">Mi Perfil</a></p>
      <th>NAME</th>
      <th>PRICE</th>
      <th>CATEGORY</th>
      <th>DESCRIPTION</th>
      <th>STOCK</th>
    </tr>
  </thead>
  <tbody>
       {{#each payload}}
        <tr>         
            <td>{{this.name}}</td>
            <td>{{this.price}}</td>
            <td>{{this.category}}</td>
            <td>{{this.description}}</td>
            <td>{{this.stock}}</td>
        <td><button class="boton1">AGREGAR PRODUCTO</button></td>
      </tr>
    {{/each}}
  </tbody>
</table>

<p>PAGE: {{page}}</p>
{{#if prevLink}}
  <a href="{{prevLink}}" role="button" class="boton1">LAST</a>
{{/if}}
{{#if nextLink}}
  <a href="{{nextLink}}" role="button" class="boton1">NEXT</a>
{{/if}}